<!-- _reviews_section.html.erb -->

<div class="reviews-section">
  <div class="rating-summary">
    <div class="average-rating">
      <% if @chef_profile.reviews.any? %>
        <span class="stars">
          <%= "⭐" * @chef_profile.reviews.average(:rating).round %>
        </span>
      <% else %>
        <span class="stars empty-stars"></span>
      <% end %>
    </div>
  </div>
</div>
