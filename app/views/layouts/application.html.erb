<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="theme-color" content="#e17055">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <!-- Page Title -->
    <title><%= content_for?(:title) ? yield(:title) : "Gusto" %></title>

    <!-- Manifest and Icons -->
    <link rel="manifest" href="/manifest.json">
    <link rel="icon" href="<%= asset_path('gusto_logo.png') %>">
    <link rel="icon" type="image/svg+xml" href="<%= asset_path('gusto_logo.png') %>">
    <link rel="apple-touch-icon" href="<%= asset_path('gusto_logo.png') %>">

    <!-- Font Awesome -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css">

    <!-- Stylesheets and Scripts -->
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>

    <!-- Additional Head Content -->
    <%= yield :head %>
  </head>

  <body>
    <%= yield %>
  </body>
</html>
